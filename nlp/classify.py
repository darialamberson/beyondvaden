# assume we have a tf-idf table
# take a natural string query of undefined length
# categorize naively, if failure, then reprompt to add information or select category

def classify_query(query, tfidf_file):
	# returns a vector 
	return ":-("

